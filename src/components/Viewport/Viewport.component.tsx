import * as React from 'react';
import {ViewportProps} from "./Viewport.d";
import './Viewport.less';

export const ViewportComponent = (props:ViewportProps) =>
    <div>Viewport component goes here.</div>;
